package edu.cslg.easyshopping.service;

import edu.cslg.easyshopping.pojo.Seller;
import edu.cslg.easyshopping.pojo.Stuff;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import javax.annotation.Resource;

@SpringBootTest
@RunWith(SpringRunner.class)
public class StuffServiceTest {

    @Resource
    private StuffService stuffService;

    @Test
    public void seller_stuff(){
/*
        Seller seller = new Seller();
        seller.setId(10101);

        Stuff newStuff = new Stuff();
        newStuff.setFaceImg("ceshi");
        newStuff.setOppositeImg("ceshi");
        newStuff.setHandImg("ceshi");
        newStuff.setStuffStatus(false);
        newStuff.setReason(null);

        newStuff.setSeller(seller);
        stuffService.saveStuff(newStuff);*/
    }
}
